"""Прикладной конфиг: идентификаторы Telegram-сущностей и бизнес-настройки.

Это НЕ секреты и они стабильны между запусками (id канала/группы не меняются
при переносе бота на другой VDS), поэтому живут в коде и коммитятся.
Репозиторий приватный — публиковать эти id не страшно.

Секреты и инфраструктура (токен, креды БД, прокси) — в .env → bot/config.py.
thread_id топиков категорий — в bot/categories.py (поле CategoryDef.thread_id).
"""

# --- Главный админ -------------------------------------------------------
ADMIN_ID: int = 0                      # tg id супер-админа

# --- Супергруппа тендеров (топики категорий из bot/categories.py) ---------
# Модерация (заявки/новые работы) идёт в ЛС ADMIN_ID, отдельного чата нет.
GROUP_ID: int = 0                      # id супергруппы (-100...)

# --- Публичный канал-зеркало отзывов -------------------------------------
REVIEWS_CHANNEL_ID: int = 0            # id канала (-100...)
REVIEWS_CHANNEL_URL: str = ""          # https://t.me/...

# --- Оплата --------------------------------------------------------------
PAYMENT_DETAILS: str = "Реквизиты не заданы (bot/app_config.py)"
